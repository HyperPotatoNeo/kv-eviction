# AIME KV-corruption experiment: attention matching plus 50% chunk shuffle.
#
# This keeps the relaxed 16K AM baseline settings and adds deterministic
# KV-cache shuffle corruption. User prompts are protected by default so the
# first prompt can serve as a stable attention sink.

seed = 0

enable_prefix_caching = false

[server]
host = "0.0.0.0"
port = 8000

[model]
name = "Qwen/Qwen3-4B-Instruct-2507"
max_model_len = 16384
enforce_eager = false

[parallel]
dp = 4
tp = 1

[vllm_extra]
block_size = 16
async_scheduling = false
compaction_window_size = 4096
compaction_stride = 1024
compaction_strategy = "attention_matching"
attention_backend = "FLEX_ATTENTION"
attention_matching_max_queries_per_kv_head = 64
attention_matching_query_source = "random_queries"
attention_matching_protect_user_prompts = "first_user"
shuffle_control_chunk_size = 512
shuffle_control_probability = 0.50
shuffle_control_seed = 0
gpu_memory_utilization = 0.4
